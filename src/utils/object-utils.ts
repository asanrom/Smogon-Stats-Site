/**
 * Object utils
 * Smogon Stats Site - https://github.com/asanrom/Smogon-Stats-Site
 *
 * Utilities for working with Javascript objects.
 */

"use strict";

/**
 * Verifies if an object is defined.
 * @param o   Any object to check.
 */
export function isDef(o: any): boolean {
  return !(o === undefined);
}
